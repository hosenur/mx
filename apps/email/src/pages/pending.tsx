import React from "react";

export default function PendingPage() {
  return (
    <div>
      it seems you have no verified domains in inbound.new, verify atleast one
      domain to continue
    </div>
  );
}
