{
  "name": "@mx/engine",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "deploy": "nitro build && bunx wrangler deploy",
    "logs": "bunx wrangler tail hosenur-mx-apps-engine"
  },
  "devDependencies": {
    "nitro": "npm:nitro-nightly@latest",
    "prisma": "^6.13.0"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "@prisma/extension-accelerate": "^2.0.2"
  }
}
